/**
 * 
 * @param { Object }   node            Node object belonging to a loop
 * @param { Function } node.getNext()  Get next set of node
 * @param { Function } node.next()     Get next set of node
 */

var loop_size = function(node){

    return node;
}

module.exports = loop_size;